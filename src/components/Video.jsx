import React from "react";

const Video = () => {
  return (
    <div
      className={`relative aspect-3/4 w-full md:aspect-auto md:h-[calc(100vh-64px)] md:min-h-0 bg-slate-500 `}
    >
      video
    </div>
  );
};

export default Video;
